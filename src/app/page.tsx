import { colors, typography, shadows, spacing } from "@/src/app/tokens";

export default function Home() {
  return (
    <div
      style={{
        padding: spacing.xl,
      }}
    >
      Padding Test
    </div>
  );
}